from django.apps import AppConfig


class WebsocketsConfig(AppConfig):
    """Default configuration for Websocket app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.websockets"
